name: Create Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  linux:
    uses: ./.github/workflows/Linux.yml

  linux-arm64:
    uses: ./.github/workflows/LinuxARM64.yml

  windows:
    uses: ./.github/workflows/Windows.yml

  freebsd:
    uses: ./.github/workflows/FreeBSD.yml

  macos:
    uses: ./.github/workflows/macOS.yml

  release:
    name: Draft release
    needs: [linux, linux-arm64, windows, freebsd, macos]
    runs-on: ubuntu-latest

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Package artifacts
        run: |
          cd artifacts

          # Linux tarballs
          tar czf prey2006-linux-x86.tar.gz -C prey2006-linux-x86 .
          tar czf prey2006-linux-x86_64.tar.gz -C prey2006-linux-x86_64 .
          tar czf prey2006-linux-arm64.tar.gz -C prey2006-linux-arm64 .

          # macOS tarballs
          tar czf prey2006-macos-arm64.tar.gz -C prey2006-macos-arm64 .
          tar czf prey2006-macos-x86_64.tar.gz -C prey2006-macos-x86_64 .

          # Windows zips
          cd prey2006-windows-x86
          zip -r ../prey2006-windows-x86.zip .
          cd ..
          cd prey2006-windows-x86_64
          zip -r ../prey2006-windows-x86_64.zip .
          cd ..

      - name: Create Pre Release
        uses: softprops/action-gh-release@v2
        with:
          prerelease: true
          generate_release_notes: true
          files: |
            artifacts/prey2006-windows-x86.zip
            artifacts/prey2006-windows-x86_64.zip
            artifacts/prey2006-linux-x86.tar.gz
            artifacts/prey2006-linux-x86_64.tar.gz
            artifacts/prey2006-linux-arm64.tar.gz
            artifacts/prey2006-macos-arm64.tar.gz
            artifacts/prey2006-macos-x86_64.tar.gz
            artifacts/prey2006-freebsd.zip
